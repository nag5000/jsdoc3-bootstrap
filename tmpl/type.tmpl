<?js
	var data = obj;
    var self = this;
    data.forEach(function(name, i) { ?>
<h5 class="param-type"><?js= self.linkto(name, self.htmlsafe(name)) ?></h5>
<?js if (i < data.length-1) { ?>|<?js } ?>
<?js }); ?>
